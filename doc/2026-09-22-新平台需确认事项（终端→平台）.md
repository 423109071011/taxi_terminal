# 新平台对接：平台侧需确认 / 调整事项

> 出租车终端 → 新平台（62.234.83.77:8082）
> 日期：2026-09-22
> 前提：**终端协议实现已随前一平台（115.29.177.207:8899）联调验收并在线运行，字段口径以终端实测帧为准**。终端侧代码不作协议性改动，以下事项请平台侧确认或调整。终端通过配置文件在两平台间随时切换，两边共用同一固件。

---

## 一、平台侧需调整 / 确认（按优先级）

### P0-1 报警位掩码：请按十六进制字节值对齐（文档 bit 序号有歧义）

你们文档 §5 写"bit1=烟雾超标、bit2=疲劳驾驶"，未说明 bit 从 0 还是从 1 编号，两种口径掩码不同。**终端实际发送的字节值（实测帧）为：**

| 报警 | 终端发送的掩码（报警标志 4B，大端） |
|---|---|
| 疲劳驾驶 | `0x00000004` |
| 烟雾超标 | `0x00000001` |
| 两者同时 | `0x00000005` |

请平台侧按上述 **hex 值**核对解析代码（不要按 bit 序号推导）。若你们解析器当前按 `0x02/0x04` 解析，请调整为上表值——这是已验收的既成实现。

### P1-1 速度字段：文档口径需更新

你们文档 §5 写"速度恒 0，待联调确认"、§7.2 要求二选一。确认结果：

- **终端上报真实 GPS 地面速度，整数 km/h（选方案一）**，超速判定留在平台侧（alarm_rule 80km/h），终端不用报警位报超速。
- 请把文档 §5"待确认"和 §7.2 的二选一描述更新为上述结论，避免后续接入车辆按旧口径实现。

### P1-2 离线判定口径：文档内部两处不一致，请统一

- §1：读空闲超时 **180 秒**判离线；
- §8 验收清单：断开终端 **30 秒**后置离线。

两处口径不一致，请统一并明确：TCP 断开是否立即置离线？读空闲 180s 是兜底还是唯一依据？（终端侧无影响，仅平台行为需要定死，便于验收时对表。）

### P2-1 真实帧样例：请平台侧从接入日志导出回传

终端接入后（每秒 1 帧 0x0200），请从平台日志导出以下几类原始 hex 帧回传核对：GPS 定位后的 0x0200、刷卡后的 0x0210、报警位触发时的 0x0200。你们 §7.3 也要求了这一项——由平台侧导出最准确，正好同步核验编解码与字段偏移。

### P2-2 装载状态（空车/重车）：确认维持"平台默认空车"

终端状态位不携带装载信息（协议未定义），确认按你们文档"平台暂默认空车"处理即可，终端无需增改。若后续要区分，请给出协议扩展定义，双方再排期。

---

## 二、已对齐、无需任何改动（备查）

| 项 | 状态 |
|---|---|
| 消息集（上行 0x0200/0x0210/0x0001；下行 0x8001/0x8110/0x8210/0x8811） | 完全一致 |
| 帧格式：22B 头（含封装项 4B 恒 0）、异或校验、7E/7D 转义（双向） | 完全一致 |
| 0x8110 语义：`0x00`=通过开门，非 00=失败 | 一致（实测验证过） |
| 0x0210：卡号 4B + 身份码 ASCII | 一致 |
| 时间字段：BCD `YYMMDDhhmmss`，UTC，入库 +8 | 一致 |
| 心跳：0x0200 每秒 1 帧 + 连接即发首帧 | 一致（180s 读空闲余量充足） |
| 状态变化即时上报（烟雾/车门/疲劳） | 终端已实现（即时多报一帧 0x0200，平台按"最新位置覆盖"处理即可） |
| 断线重连：2 秒退避 | 满足 3~5s 建议 |
| 文档 §6 六条联调固定帧 | 校验码已逐条验算，全部正确，可直接使用 |
| 文档 §9 三条 HTTP 指令 | 与 0x8811/0x8210/0x8110 一一对应，直接可用 |
| 不要求 0x0100 注册 / 0x0102 鉴权 | 与终端现状一致，无冲突 |

---

## 三、终端侧信息（平台侧备查）

- 终端 ID / 手机号（BCD 6B）：`091111222200`，映射 浙A·T3236 / ZD2026001001（已在 device-aliases 配好）
- 终端为 TCP 客户端，主动连接 `62.234.83.77:8082`；固件含即时上报功能版本 md5：`72c9f815`
- 多平台切换：终端仅改本地配置（IP/端口）重启即接入，切换不影响协议行为
